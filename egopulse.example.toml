model = "gpt-4o-mini"
# Localhost-style endpoints such as LM Studio may omit api_key.
api_key = "replace-me"
base_url = "https://api.openai.com/v1"
# Session and message history are stored under data_dir/egopulse.db.
data_dir = ".egopulse"
log_level = "info"

# OpenAI-compatible router endpoint example:
# model = "openai/gpt-4o-mini"
# api_key = "sk-or-..."
# base_url = "https://openrouter.ai/api/v1"

# Local OpenAI-compatible endpoint example:
# model = "local-model"
# base_url = "http://127.0.0.1:1234/v1"
