# Plan: <タイトル>

<1-2文の概要>

> **Note**: 振る舞い（What）は決して変えてはいけないが、より美しい設計があれば実装方法（HOW）だけは変えてもよい。

## 設計方針

- <既存パターン・責務境界・採用する設計判断>
- <実装詳細を固定しすぎず、TDD のフィードバックで調整する方針>
- <関連 docs / 既存テスト / 外部実装など参照元>

## TDD 方針

<この Plan で守る TDD の進め方を 2-4 文で書く。テストリスト項目と自動テストを区別し、Red / Green / Refactor を混ぜないことを明記する。「1回のRedでは自動テスト1件」と「1項目に必要なテスト総数は1件とは限らない」を区別し、実装中に見つかった不安はテストリストへ追加してCycleを続ける方針も含める。>

## Plan スコープ

WT作成 → 実装(TDD) → コミット(意味ごとに分離) → PR作成

## 対象一覧

| 対象 | 種別 | 既存パターン / 参照元 | 備考 |
| -- | -- | -- | -- |
| <file/module> | 変更 | <既存テスト・docs> | <責務や注意点> |

## テストリスト / 不安リスト

| ID | 観点 | 期待する振る舞い | 優先 | 対応するCycle | 状態 / 今回対象外理由 |
| -- | -- | -- | -- | -- | -- |
| T1 | 正常系 | <外から見た期待振る舞い> | High | Step 1 | 未着手 |
| T2 | 境界値 | <外から見た期待振る舞い> | High | Step 2 | 未着手 |
| T3 | 異常系 | <外から見た期待振る舞い> | Medium | Step 3 / 今回対象外 | <理由> |

---

## Step 0: Worktree 作成

- ブランチ名: `<type>/<desc>`
- 作成コマンド:
  - `git worktree add ../<wt-name> -b <type>/<desc>`

---

## Step 1: <モジュール> TDD Cycle - <T1の短い説明>

### この Step の目的

<この Cycle で実現したい振る舞い・状態を1-2文で書く>

### 今回選ぶ項目

- 対象: `T1`
- 選ぶ理由: <最も小さく、価値があり、次の設計判断につながるため>
- この時点では扱わないこと: <T2以降、統合、最適化など>

### RED: 失敗する自動テストを書く

- 追加するテスト名: `test_name`
- Given:
- When:
- Then:
- 失敗理由の想定: <未実装 / 既存仕様との不一致 / 境界未対応など>

### GREEN: 最小実装

<Red で追加した自動テストを通すための最小実装方針。過剰な一般化・先回り設計は禁止。>

### REFACTOR: 設計の整理

- 重複: <確認内容>
- 命名: <確認内容>
- 責務: <確認内容>
- テストの構造的結合: <private/internal に寄りすぎていないか>
- 次の項目へ進める身軽さ: <次候補に進める状態か>

### テストリスト更新

- 完了: `T1`
- 追加: <Green/Refactor 中に見つかった不安。なければ「なし」>
- 次候補: `T2`

### コミット

`<type>: <description>`

<!-- Step 2-N は Step 1 と同じ構造で、実装対象のテストリスト項目ごとに追加する。 -->
<!-- 1つのテストリスト項目に複数ケースが必要なら、同じ項目を対象にしたCycleを複数作る。各CycleのRedでは自動テストを1件だけ追加する。 -->

---

## Step N+1: 動作確認

- 全テスト通過コマンド
- Lint / フォーマット / 型チェック
- 必要に応じた手動確認
- 失敗時に戻る Step

---

## Step N+2: Plan・仕様書との自己チェック

実装完了後にこの Plan と関連仕様書を最初から読み直し、実装・自動テスト・文書が要求した振る舞いと一致しているかを照合する。未実装、過剰実装、テスト不足、仕様書との齟齬を見つけた場合は、該当する TDD Cycle へ戻って修正し、動作確認を再実行してからこの Step を完了する。

- Plan のテストリストと各 Cycle が完了条件を満たしている
- 関連仕様書の What と実装結果が一致している
- 実装中に変更した設計判断が関連 docs へ反映されている
- 変更ファイル一覧、コミット分割、自動テスト一覧が実際の変更と一致している

---

## Step N+3: PR 作成

- PR タイトル:
- PR description:
  - 概要
  - テスト
  - Close #<issue-number>（該当する場合）

---

## 変更ファイル一覧

| ファイル | 変更種別 | 内容 |
| ---- | ---- | -- |
| <path> | **新規** / 変更 | <内容> |

---

## コミット分割

1. `<type>: <description>` - <対象ファイル / 論理的変更>
2. `<type>: <description>` - <対象ファイル / 論理的変更>

---

## 自動テスト一覧（全 N 件）

この一覧はPlan作成時点で必要と判断した最低限の予定であり、最終テスト件数の上限ではない。実装中に追加された不安には、テストリストとTDD Cycleを追加して対応する。

### <モジュール>（全 X 件）

| テストリストID | 自動テスト名 | 追加Step | 実行コマンド |
| -- | -- | -- | -- |
| T1 | `test_name` | Step 1 | `<command>` |
| T2 | `test_name` | Step 2 | `<command>` |

---

## 工数見積もり

| Step | 内容 | 見積もり |
| -- | -- | -- |
| Step 1 | <TDD Cycle> | ~N 行 |
| Step 2 | <TDD Cycle> | ~N 行 |
| Step N | 動作確認 / Plan・仕様書との自己チェック / PR 作成 | ~N 行 |
| **合計** |  | **~N 行** |
